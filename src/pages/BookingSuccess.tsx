import { CheckCircle2 } from "lucide-react";
import { Link, useSearchParams } from "react-router-dom";
export default function BookingSuccess(){const [params]=useSearchParams();const code=params.get("code");const queue=params.get("queue");return <div className="success-page"><div className="success-card"><CheckCircle2 size={64}/><div className="eyebrow">BOOKING BERHASIL</div><h1>Jadwalmu sudah tercatat.</h1><p>Simpan kode booking dan nomor antrean berikut.</p><div className="code">{code||"BOOKING"}</div>{queue&&<div className="queue-highlight"><small>NOMOR ANTREAN</small><strong>{queue}</strong></div>}<div className="actions"><Link className="btn primary" to="/">Kembali ke Beranda</Link></div></div></div>}
